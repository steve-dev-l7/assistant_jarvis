<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer glowing gradient stroke -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="3dp"
                android:color="@android:color/transparent" />

            <!-- Jarvis futuristic gradient border -->
            <gradient
                android:angle="135"
                android:startColor="#00F5FF"
                android:centerColor="#00D9FF"
                android:endColor="#007BFF"
                android:type="linear" />

            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Inner background (dark futuristic) -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="3dp">
        <shape android:shape="rectangle">
            <solid android:color="#0D1B2A" /> <!-- Dark background -->
            <corners android:radius="10dp" />
        </shape>
    </item>

</layer-list>
