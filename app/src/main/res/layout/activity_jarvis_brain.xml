<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Particle animation canvas -->
    <com.example.translateanywhere.ParticleView
        android:id="@+id/particleView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Back button (top-left) -->
    <ImageButton
        android:id="@+id/btnBrainBack"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|start"
        android:layout_margin="20dp"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_arrow_back"
        android:contentDescription="Back"
        android:tint="#FFCC44" />

    <!-- Title overlay -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="28dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="JARVIS BRAIN"
            android:textColor="#FFCC44"
            android:textSize="22sp"
            android:textStyle="bold"
            android:letterSpacing="0.3"
            android:fontFamily="sans-serif-medium"
            android:shadowColor="#FF8800"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="18" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NEURAL NETWORK VISUALIZER"
            android:textColor="#FFAA33"
            android:textSize="11sp"
            android:letterSpacing="0.2"
            android:layout_marginTop="4dp"
            android:alpha="0.75" />
    </LinearLayout>

    <!-- Bottom status bar -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="28dp"
        android:text="● SYNAPTIC ACTIVITY LIVE"
        android:textColor="#FFAA33"
        android:textSize="11sp"
        android:letterSpacing="0.2"
        android:gravity="center"
        android:alpha="0.7" />

</FrameLayout>
